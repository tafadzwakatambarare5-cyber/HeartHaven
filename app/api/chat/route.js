import OpenAI from "openai";
import {NextResponse} from "next/server";
const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});
export async function POST(req){try{const{messages=[]}=await req.json();const safe=messages.slice(-12).map(m=>({role:m.role==="assistant"?"assistant":"user",content:String(m.content).slice(0,4000)}));const response=await client.responses.create({model:"gpt-5-mini",instructions:"You are Haven AI inside HearthHaven, a calm supportive wellbeing companion. Be warm, concise, non-judgmental and age-appropriate. Help users name feelings, reflect, organise thoughts and choose small practical next steps. Do not claim to be a therapist or human. Encourage reaching out to a trusted adult when appropriate. Never provide instructions for self-harm, dangerous activities, or hiding distress. If the user may be in immediate danger, encourage contacting a trusted adult or local emergency services.",input:safe});return NextResponse.json({reply:response.output_text});}catch(e){console.error(e);return NextResponse.json({reply:"I'm having trouble connecting right now. Please try again."},{status:500});}}
